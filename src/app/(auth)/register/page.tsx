import { RegisterForm } from "@/components/features/auth";

export const metadata = {
  title: "Daftar | Boothable",
  description: "Daftar akun baru di Boothable",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
